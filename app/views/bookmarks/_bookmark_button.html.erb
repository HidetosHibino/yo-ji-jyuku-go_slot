<% if current_user&.bookmark?(bookmarkable) %>
  <%= render 'bookmarks/unbookmark', { bookmarkable: bookmarkable } %>
<% else %>
  <%= render 'bookmarks/bookmark', { bookmarkable: bookmarkable } %>
<% end %>